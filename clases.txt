class Documento:

	def __init__(self,fichero):
		paginas = []
		paginaActual = Pagina()
		documento.__add(paginaActual)

		lineaFormato = leerLinea()

		linea = leerLinea()

		while linea != “”

			if __esSeparador(linea):
				paginaActual = Pagina()
				documento.__add(paginaActual)
			else:
				paginaActual.add(linea)

	def __add(self,pagina):
		paginas.append(pagina)

	def getNumberOfPages(self):
		size(self.pages)

	def getPage(self,n):
		return self.paginas[n]

	def __esSeparador(self,linea):
		return linea == “..........”

class Pagina:
	def __init__(self):
		lineas = []

	def add(self,linea):
		lineas.append(linea)




doc = Documento(“/home/jose/fichero.txt”)

if (doc.getNumPages >0)
	pag = doc.getPage(0)
